# Database Configuration
# For local development (PostgreSQL):
# DATABASE_URL="postgresql://user:password@localhost:5432/zielvereinbarung?schema=public"
# DATABASE_URL_UNPOOLED="postgresql://user:password@localhost:5432/zielvereinbarung?schema=public"

# For local development (SQLite - alternative):
# DATABASE_URL="file:./dev.db"
# DATABASE_URL_UNPOOLED="file:./dev.db"
# Note: Change provider in schema.prisma to "sqlite"

# For production (Vercel Postgres):
# Vercel setzt diese automatisch:
# POSTGRES_PRISMA_URL (pooled connection)
# POSTGRES_URL_NON_POOLING (direct connection)
DATABASE_URL="${POSTGRES_PRISMA_URL}"
DATABASE_URL_UNPOOLED="${POSTGRES_URL_NON_POOLING}"

# Authentication Secret
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here"

# Admin Credentials
# IMPORTANT: Change these for production!
ADMIN_EMAIL="admin@schulaufsicht.nrw"
ADMIN_PASSWORD="change-this-password"
# ADMIN_ROLE: Optional, defaults to "ADMIN". Set to "SUPERADMIN" for user management access
ADMIN_ROLE="ADMIN"

# Node Environment
NODE_ENV="production"
